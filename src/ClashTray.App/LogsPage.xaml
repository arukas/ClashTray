<UserControl
    x:Class="ClashTray.App.LogsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style TargetType="ListViewItem">
            <Setter Property="Padding" Value="8,7" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        </Style>
    </UserControl.Resources>

    <StackPanel Spacing="10" Padding="0,2,0,12">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <FontIcon Glyph="&#xE8A5;" FontSize="20" Foreground="{ThemeResource AccentFillColorDefaultBrush}" VerticalAlignment="Center" />
            <StackPanel Grid.Column="1" Margin="10,0,8,0" Spacing="1">
                <TextBlock x:Uid="LogsTitle" FontSize="20" FontWeight="SemiBold" />
                <TextBlock x:Uid="LogsSubtitle" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" TextWrapping="Wrap" />
            </StackPanel>
            <Button Grid.Column="2" x:Uid="CopyLogsButton" Style="{StaticResource ClashTrayCompactButtonStyle}" Click="CopyButton_Click" Margin="4,0,0,0" />
            <Button Grid.Column="3" x:Uid="ClearLogsButton" Style="{StaticResource ClashTrayCompactButtonStyle}" Click="ClearButton_Click" Margin="4,0,0,0" />
        </Grid>

        <Border Style="{StaticResource ClashTrayCardStyle}">
            <Grid ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox x:Name="SearchBox" x:Uid="LogsSearchBox" TextChanged="SearchBox_TextChanged" />
                <ComboBox x:Name="LevelBox" Grid.Column="1" SelectedIndex="0" SelectionChanged="LevelBox_SelectionChanged" MinWidth="84">
                    <ComboBoxItem x:Uid="LevelAllItem" Tag="all" />
                    <ComboBoxItem x:Uid="LogLevelInfoItem" Tag="info" />
                    <ComboBoxItem x:Uid="LogLevelWarningItem" Tag="warning" />
                    <ComboBoxItem x:Uid="LogLevelErrorItem" Tag="error" />
                </ComboBox>
                <ComboBox x:Name="SourceBox" Grid.Column="2" SelectedIndex="0" SelectionChanged="SourceBox_SelectionChanged" MinWidth="100">
                    <ComboBoxItem x:Uid="SourceAllItem" Tag="all" />
                    <ComboBoxItem x:Uid="SourceClashTrayItem" Tag="ClashTray" />
                    <ComboBoxItem x:Uid="SourceMihomoItem" Tag="mihomo" />
                </ComboBox>
            </Grid>
        </Border>

        <Border Style="{StaticResource ClashTrayCardStyle}" Padding="8"><Grid MinHeight="80"><ListView x:Name="LogsListView" x:Uid="LogsListView" ScrollViewer.VerticalScrollMode="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled" ScrollViewer.HorizontalScrollMode="Disabled" ScrollViewer.HorizontalScrollBarVisibility="Disabled" /><TextBlock x:Name="EmptyListText" x:Uid="LogsEmptyListText" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="13" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/></Grid></Border>
    </StackPanel>
</UserControl>
