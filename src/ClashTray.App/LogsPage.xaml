<UserControl
    x:Class="ClashTray.App.LogsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style TargetType="ListViewItem">
            <Setter Property="Padding" Value="8,7" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        </Style>
    </UserControl.Resources>

    <StackPanel Spacing="10" Padding="0,2,0,12">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <FontIcon Glyph="&#xE8A5;" FontSize="20" Foreground="{ThemeResource AccentFillColorDefaultBrush}" VerticalAlignment="Center" />
            <StackPanel Grid.Column="1" Margin="10,0,8,0" Spacing="1">
                <TextBlock Text="日志" FontSize="20" FontWeight="SemiBold" />
                <TextBlock Text="实时查看 ClashTray 与 Mihomo 运行记录" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" TextWrapping="Wrap" />
            </StackPanel>
            <Button Grid.Column="2" Content="复制" Style="{StaticResource ClashTrayCompactButtonStyle}" Click="CopyButton_Click" Margin="4,0,0,0" />
            <Button Grid.Column="3" Content="清空" Style="{StaticResource ClashTrayCompactButtonStyle}" Click="ClearButton_Click" Margin="4,0,0,0" />
        </Grid>

        <Border Style="{StaticResource ClashTrayCardStyle}">
            <Grid ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox x:Name="SearchBox" PlaceholderText="搜索日志内容" TextChanged="SearchBox_TextChanged" />
                <ComboBox x:Name="LevelBox" Grid.Column="1" SelectedIndex="0" SelectionChanged="LevelBox_SelectionChanged" MinWidth="84">
                    <ComboBoxItem Content="全部" />
                    <ComboBoxItem Content="info" />
                    <ComboBoxItem Content="warning" />
                    <ComboBoxItem Content="error" />
                </ComboBox>
                <ComboBox x:Name="SourceBox" Grid.Column="2" SelectedIndex="0" SelectionChanged="SourceBox_SelectionChanged" MinWidth="100">
                    <ComboBoxItem Content="全部来源" />
                    <ComboBoxItem Content="ClashTray" />
                    <ComboBoxItem Content="mihomo" />
                </ComboBox>
            </Grid>
        </Border>

        <Border Style="{StaticResource ClashTrayCardStyle}" Padding="8"><Grid MinHeight="80"><ListView x:Name="LogsListView" AutomationProperties.Name="日志列表" ScrollViewer.VerticalScrollMode="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled" ScrollViewer.HorizontalScrollMode="Disabled" ScrollViewer.HorizontalScrollBarVisibility="Disabled" /><TextBlock x:Name="EmptyListText" Text="暂无匹配的日志" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="13" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/></Grid></Border>
    </StackPanel>
</UserControl>
