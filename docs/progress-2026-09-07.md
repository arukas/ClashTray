# ClashTray 今日开发进度

日期：2026-09-07

## 今日完成

已按照 `AGENTS.md` 的 DEFAULT IMPLEMENTATION REQUIREMENT 完成 P0 + P1 主体开发，P2 暂不实现。

- Windows-only .NET 10 + WinUI 3 应用结构与解决方案。
- 单实例托盘应用、托盘状态图标、左键飞出面板、右键快捷菜单、固定窗口和显式退出。
- Mihomo 核心发现、配置验证、启动、停止、重启、崩溃检测与控制器重连。
- 本地 YAML/YML 配置、订阅导入、选择、刷新、重载和删除。
- Rule/Global/Direct 模式、代理组、节点切换和延迟测试。
- System Proxy 备份、所有权检测、冲突保护、WinINet 通知、恢复和卸载恢复路径。
- Windows Service + 受限命名管道，用于 Mihomo 生命周期和 TUN 操作。
- TUN 状态确认、失败处理和服务不可用状态。
- 规则、连接、Provider、流量、内存和实时日志页面。
- 日志来源/级别/搜索过滤、复制、清空、限量保留和重复行折叠。
- 开机启动、自动启动核心、HTTP/SOCKS/Mixed/Controller 端口设置。
- allow-lan、IPv6、TCP concurrent、log-level、主题和系统代理绕过列表设置。
- DNS/FakeIP 缓存清理、Geo 数据库更新和 Provider 手动刷新。
- 订阅定时刷新。
- 设置页可配置订阅刷新间隔（1–168 小时），并在运行时校验范围。
- 设置页端口输入支持整数范围校验和重复端口提示，非法输入不会直接中断保存事件。
- 对齐 Mihomo 官方控制器 API：FakeIP 清理使用 POST，规则列表兼容官方对象返回格式。
- 官方 MetaCubeX/Mihomo 下载源校验、SHA-256 校验、原子替换和回滚。
- MSIX 打包、原始应用图标、托盘状态图标和 packaged Windows Service 配置。
- 运行时目录 ACL、DPAPI 控制器密钥和服务退出时的系统代理恢复。

## 验证结果

- 完整 Debug 构建：0 警告、0 错误。
- 自动化测试：12/12 通过。
- 命名管道服务 GetStatus smoke test：通过。
- Release MSIX：MakeAppx 创建成功。
- Release 包路径：`packaging\\out\\ClashTray-Release.msix`
- Release 包 SHA-256：`231544F9DDA0D933664DA3531093CC8879A79BD114F4D0B63AD2D0B6FE1AC33C`

## 重要文件

- `src/ClashTray.App/`：WinUI 3 托盘应用和页面。
- `src/ClashTray.Core/ClashTrayRuntime.cs`：核心运行时编排。
- `src/ClashTray.Core/SystemProxyManager.cs`：系统代理状态和所有权管理。
- `src/ClashTray.Core/SystemProxyRecovery.cs`：服务退出时的用户代理恢复。
- `src/ClashTray.Service/`：Windows Service 和命名管道边界。
- `packaging/ClashTray.Package/Package.appxmanifest`：MSIX 与 packaged service 声明。
- `packaging/Build-MSIX.ps1`：Release/Debug 打包脚本，支持 `-PackageVersion`。
- `docs/setup.md`：构建、打包和运行说明。
- `docs/architecture.md`：架构和安全边界说明。

## 明日验收清单

1. 启动应用，确认托盘图标和状态切换。
2. 左键打开面板，确认位置贴近托盘图标；测试固定/取消固定和退出。
3. 导入一份有效 Mihomo YAML，启动核心并确认版本、流量、连接数和内存显示。
4. 切换规则/全局/直连，切换代理组节点并执行延迟测试。
5. 开启/关闭 System Proxy，验证原有 Windows 代理设置能够恢复。
6. 打开 TUN；确认未安装服务时显示“服务未安装”，安装 packaged MSIX 后再验证启停。
7. 检查规则、连接、Provider、日志页面的搜索、过滤、复制、关闭和刷新操作。
8. 修改端口、allow-lan、IPv6、TCP concurrent、log-level、主题和开机启动设置。
9. 测试 DNS/FakeIP/Geo 操作、订阅刷新和 Mihomo 核心更新表单校验。
10. 测试 Explorer 重启、核心崩溃、服务重启、代理冲突、DPI、多显示器、暗色/亮色和高对比度。
11. 进行一次签名后的 MSIX 安装、升级和卸载验收。

## 已知验证限制

本开发环境无法使用原生 Windows UI 自动化接口，因此今天没有完成实际托盘点击、MSIX 安装/卸载对话框、多显示器和高 DPI 的人工操作验证。Release MSIX 当前未签名、未直接安装；代码、构建、测试、命名管道和打包流程已验证通过。
