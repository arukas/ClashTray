# 2026-09-08 checkpoint: compact tray flyout

## Saved work

- EXE installer, bundled official Mihomo v1.19.30, service path Unicode fix, setup diagnostics and WinUI merged resource dictionary fix from the previous install/debug iterations.
- Reworked panel into one compact surface: header, full-width mode selector, underline navigation, bounded live traffic chart, configuration flyout, right-aligned System Proxy/TUN switches, collapsible providers and single-line proxy groups.
- Corrected notification-area v4 packed messages, Unicode Shell_NotifyIconW, persistent icon identity, first-launch hide and foreground activation.
- Corrected MONITORINFO layout (including dwFlags). Position relative to the actual tray monitor's work area, with 8 DIP corner inset and size clamping for smaller work areas.
- Added isolated --ui-smoke-test=<absolute output directory> mode. It uses synthetic data and isolated storage; does not initialize networking or register an additional tray icon.

## Verification at checkpoint

- Full Release solution build succeeds. A CS0252 reference-comparison warning remains in MainWindow.SmokeTest.cs; it is in the diagnostic navigation assertion, not the application path.
- Tests: 41 passed (39 core, 2 integration).
- Native monitor query, bottom/top/left/right taskbars, negative-origin monitors, high-DPI sizing, tiny work areas and v4 callback decoding covered by regression tests.
- Real WinUI render and in-process window checks completed: startup hidden; toggle show/hide; deactivation hides; navigation to all five pages; empty/populated and light/dark rendering.
- Evidence: artifacts/ui-v5/{empty-light,empty-dark,nodes-light,nodes-dark}.png and geometry.json.
- Measured window: (2132,704)-(2552,1384), work area (0,0)-(2560,1392); right and bottom insets both 8 physical pixels at 100% scaling.

## Resume tomorrow

User requested to stop iteration here and commit. No new EXE was built after this UI/positioning iteration. packaging/out-fixed contains the earlier startup-fix package, NOT the current redesign.

1. Clean up the diagnostic comparison warning.
2. Continue actual Explorer tray mouse/keyboard clicks, popup focus handling (configuration flyout/file dialog), pin/unpin and live core/node operations.
3. Verify on an actual mixed-DPI secondary monitor and in high contrast.
4. Build a distinctly named/versioned installer from the saved source; validate its staged app before install/upgrade testing.

Desktop Computer Use could not initialize (sandbox helper setup refresh errors). The screenshots are real app-owned WinUI RenderTargetBitmap captures; they do not capture DWM shadow/corners or prove real Explorer mouse/keyboard delivery. UI samples are synthetic and must not be represented as a running user's proxy data.
